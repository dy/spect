# spect-local

Persist element state in local storage.

```js
import $ from 'spect'
import 'spect-local'

// get
$('#my-element').local('foo.bar.baz')

// set
$('#my-element').local('foo.bar.baz', 'qux')
```

[WIP]
